# Kernel_neuro Clean Room Actor

Clean-room API-compatible implementation of the kernel_neuro frontier technology, backed by Datomic and Py Kotodama WASM.

## Provenance

Relocated 2026-07-04 from `etzhayyim/root/20-actors/kernel_neuro-compat` to
`kotoba-lang/com-kernel-neuro` per the org-taxonomy library-placement rule (any
library/substrate code belongs in `kotoba-lang`, ADR-2606302300), following
the same relocation pattern as `kami-nv-compat` (ADR-2607020130). See
ADR-2607041500 for the full ~1,027-repo migration plan and naming convention.
